name: 🐧 Build Linux-TkG Kernel (GCC Stable)

on:
  workflow_dispatch:
    inputs:
      force_build:
        description: '🔨 强制重新编译'
        required: false
        default: 'false'
        type: boolean
  schedule:
    - cron: '0 10 1,15 * *' # 每月 1 号和 15 号 UTC 10:00 (北京时间 18:00)

env:
  TKG_DIR: linux-tkg
  DEBIAN_FRONTEND: noninteractive

jobs:
  prepare-and-build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔍 Check Latest Stable Version
        id: check_version
        run: |
          chmod +x check_version-gcc-stable.sh
          export FORCE_BUILD="${{ github.event.inputs.force_build }}"
          ./check_version-gcc-stable.sh

      - name: ⏭️ Skip Build if Up-to-date
        if: env.SKIP_BUILD == 'true'
        run: echo "✅ GCC 稳定版已是最新 (${{ env.NEW_VERSION }})，跳过构建。" && exit 0

      - name: 🧹 Free Disk Space
        if: env.SKIP_BUILD != 'true'
        uses: rokibhasansagar/slimhub_actions@main

      - name: 📅 Generate Timestamp
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "BUILD_TIMESTAMP=$(date -u +"%Y%m%d%H%M")" >> $GITHUB_ENV
          echo "⏰ Build Timestamp: $(date -u +"%Y-%m-%d %H:%M:%S UTC")"

      - name: 📦 Install Base Dependencies (Ubuntu Official)
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "::group::📦 Installing Base Dependencies"
          sudo apt-get update
          # 使用 Ubuntu 官方源安装所有基础依赖，确保与系统核心库兼容
          # 注意：这里故意不包含 gcc/clang，因为我们要后面手动指定版本
          sudo apt-get install -y \
            bc bison build-essential ccache cpio fakeroot flex git kmod \
            libdw-dev libelf-dev libncurses-dev libssl-dev lz4 \
            rsync schedtool wget zstd debhelper bsdutils \
            qtbase5-dev dpkg-dev libqt5core5a libqt5gui5 libqt5widgets5
          echo "::endgroup::"

      - name: ⚡ Install GCC from Debian Sid (Wild Mode)
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "::group::⚡ Installing GCC from Debian Sid"
          
          # 1. 添加 Debian Sid 源 (trusted=yes 跳过密钥验证，CI 环境专用)
          echo "deb [trusted=yes] http://deb.debian.org/debian sid main" | sudo tee /etc/apt/sources.list.d/debian-sid.list
          
          # 2. 更新源
          sudo apt-get update
          
          # 3. 动态获取最新 GCC 大版本 (从 GitHub Tags)
          LATEST_GCC_VER=$(curl -s https://github.com/gcc-mirror/gcc/tags | \
                           grep -oP 'releases/gcc-\K[0-9]+(?=\.[0-9]+\.[0-9]+)' | \
                           head -n 1)
          
          if [ -z "$LATEST_GCC_VER" ]; then
            echo "⚠️ Failed to detect latest GCC, defaulting to 16."
            LATEST_GCC_VER="16"
          fi
          
          echo "✅ Target GCC Version: ${LATEST_GCC_VER}"
          echo "GCC_VER=${LATEST_GCC_VER}" >> $GITHUB_ENV
          
          # 4. 【狂野】安装 GCC 相关组件
          sudo apt-get install -y  \
            gcc-${LATEST_GCC_VER} \
            g++-${LATEST_GCC_VER} \
            cpp-${LATEST_GCC_VER}
          
          # 5. 使用 update-alternatives 切换系统默认编译器
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-${LATEST_GCC_VER} 100
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-${LATEST_GCC_VER} 100
          sudo update-alternatives --set gcc /usr/bin/gcc-${LATEST_GCC_VER}
          sudo update-alternatives --set g++ /usr/bin/g++-${LATEST_GCC_VER}
          
          # 6. 验证
          echo "🔍 Compiler Verification:"
          gcc --version | head -n 1
          g++ --version | head -n 1
          
          echo "::endgroup::"

      - name: ⚙️ Optimize Git
        if: env.SKIP_BUILD != 'true'
        run: |
          git config --global http.postBuffer 524288000
          git config --global core.compression 9
          git config --global advice.detachedHead false

      - name: 📂 Clone Linux-TkG
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "::group::📂 Cloning TKG"
          git clone --depth 1 https://github.com/Frogging-Family/linux-tkg.git $TKG_DIR
          echo "::endgroup::"

      - name: 🤖 Auto-Configure BORE
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "::group::🤖 BORE Configuration"
          if [ -f "./auto_bore_setup.sh" ]; then
            chmod +x ./auto_bore_setup.sh
            export CFG_FILE="./customization-gcc-stable.cfg"
            ./auto_bore_setup.sh
          else
            echo "⚠️ auto_bore_setup.sh not found, skipping."
          fi
          echo "::endgroup::"

      - name: 📝 Prepare & Inject Config
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "::group::📝 Final Config Prep"
          cd $TKG_DIR
          cp ../customization-gcc-stable.cfg ./customization.cfg
          
          # 【关键】通过 sed 强制注入配置，确保 install.sh 读到正确设置
          sed -i 's/^_compiler=.*/_compiler="gcc"/' customization.cfg
          sed -i 's/^_lto_mode=.*/_lto_mode="none"/' customization.cfg
          # 留空 CUSTOM_GCC_PATH，因为我们已经通过 update-alternatives 修改了系统默认 gcc
          sed -i 's|^CUSTOM_GCC_PATH=.*|CUSTOM_GCC_PATH=""|' customization.cfg
          sed -i 's/^_llvm_ias=.*/_llvm_ias=""/' customization.cfg
          
          ACTUAL_SCHEDULER=$(grep '^_cpusched=' ./customization.cfg | head -n 1 | sed 's/_cpusched=//; s/"//g; s/^ *//; s/ *$//')
          echo "CPU_SCHEDULER=${ACTUAL_SCHEDULER}" >> $GITHUB_ENV
          
          echo "--- 📋 Final Configuration ---"
          grep -E "_version|_cpusched|_compiler|_lto_mode|CUSTOM_GCC_PATH" ./customization.cfg
          echo "-------------------------------"
          echo "::endgroup::"

      - name: 🔨 Compile Kernel
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "::group::🔨 Kernel Compilation"
          cd $TKG_DIR
          
          # 【关键】直接删除 TKG 脚本中的依赖安装行，防止它再次尝试 apt install
          sed -i '/^sudo apt install/d' install.sh
          
          # 【关键】导出 CC/CXX 变量，确保 make 使用正确的编译器
          export CC=gcc-${GCC_VER}
          export CXX=g++-${GCC_VER}
          
          echo "🚀 Starting Compilation..."
          echo "   Compiler: ${CC} (Debian Sid GCC ${GCC_VER})"
          echo "   Scheduler: ${CPU_SCHEDULER}"
          echo "   Version: ${NEW_VERSION}"
          
          # 使用 sudo -E 保留环境变量
          sudo -E script -qec './install.sh install' /dev/null
          
          echo "::endgroup::"

      - name: 🗑️ Cleanup
        if: always()
        run: |
          echo "::group::🗑️ Cleanup"
          if [ -d "$TKG_DIR/linux-src-git" ]; then
            echo "Removing linux-src-git..."
            sudo rm -rf $TKG_DIR/linux-src-git
          fi
          if [ -d "$TKG_DIR/DEBS" ]; then
            echo "Removing debug and libc-dev packages..."
            sudo rm -f $TKG_DIR/DEBS/*dbg*.deb
            sudo rm -f $TKG_DIR/DEBS/*libc-dev*.deb
            echo "Remaining packages:"
            ls -lh $TKG_DIR/DEBS/
          fi
          echo "::endgroup::"

      - name: 🏷️ Create Release
        if: env.SKIP_BUILD != 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: gcc-stable-v${{ env.NEW_VERSION }}-${{ env.BUILD_TIMESTAMP }}
          name: Linux-TkG GCC Stable ${{ env.NEW_VERSION }}
          body: |
            Automated build of Linux-TkG kernel with **Latest GCC**.
            
            **Configuration:**
            - Scheduler: **${{ env.CPU_SCHEDULER }}**
            - Compiler: **GCC ${{ env.GCC_VER }} (Debian Sid)**
            - LTO: None
            - Arch Optimization: x86-64-v3
            - Kernel Version: ${{ env.NEW_VERSION }}
            
            **Notes:**
            - Optimized for stability and driver compatibility.
            - Built with Debian Sid GCC on Ubuntu Runner.
          files: |
            ${{ env.TKG_DIR }}/DEBS/*.deb
            ${{ env.TKG_DIR }}/kernelconfig.new
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 💾 Commit Config
        if: env.VERSION_CHANGED == 'true'
        run: |
          echo "::group::💾 Git Commit"
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add customization-gcc-stable.cfg
          git commit -m "chore: update gcc stable version to ${{ env.NEW_VERSION }}" || echo "No changes"
          git push
          echo "::endgroup::"